<section class="content">
  <div class="panel">
    <div class="row">
      <div class="col-md-12">
        <div class="col-md-4 content-header">
          <h1>Time Sheet Status</h1><br />
        </div>
        <div class="col-md-4 content-header">
        </div>
        <div class="col-md-3 content-header">
        </div>
        <div class="header-border"></div>
      </div>
    </div>
    <div class="row">
      <div class="col-md-5">
        <p align="center">Time Sheet Status create</p>
        <%= form_for :time_sheet_status, url: seed_table_create_time_sheet_status_path do |f| %>
        <div class="modal-body">
          <div class="errors"></div>
          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, required: true, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :short_name %>
            <%= f.text_field :short_name, required: true, class: 'form-control' %>
          </div>
        </div>
        <%= f.submit class: "btn btn-primary pd-10" %>
        <%end%>
      </div>
      <div class="col-md-7">
        <p align="center">Time Sheet Status List</p>
        <table class="table">
          <thead>
            <tr>
              <th>Time Sheet Status Name</th>
              <th>Time Sheet Status Short Name</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% @time_sheet_statuses.each do |time_sheet_status| %>
            <tr>
              <td><%= time_sheet_status.name %></td>
              <td><%= time_sheet_status.short_name %></td>
              <td class="action-link">
                <a href="" data-toggle="modal" data-target="#time_sheet_status_id_<%= time_sheet_status.id%>">
                  <i class="fa fa-user-circle-o"></i>
                </a>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>

<% @time_sheet_statuses.each do |time_sheet_status| %>
  <!-- Modal -->
  <div id="time_sheet_status_id_<%= time_sheet_status.id%>" class="modal fade" role="dialog">
    <div class="modal-dialog">

      <!-- Modal content-->
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
          <h4 class="modal-title">Time Sheet Status</h4>
        </div>
        <div class="modal-body">
          <p>Time Sheet Status Name : <span><%= time_sheet_status.name %></span></p>
          <p>Time Sheet Status Short Name : <span><%= time_sheet_status.short_name %></span></p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        </div>
      </div>

    </div>
  </div>
<% end %>

