<section class="content">
  <div class="panel">
    <div class="row">
      <div class="col-md-12">
        <div class="col-md-4 content-header">
          <h1>Employement Certificate</h1><br />
        </div>
        <div class="col-md-4 content-header">
        </div>
        <div class="col-md-3 content-header">
        </div>
        <div class="header-border"></div>
      </div>
    </div>
    <div class="row">
      <div class="col-md-5">
        <p align="center">Employement Certificate Create</p>
        <%= form_for :employment_certificate, url: seed_table_create_employment_certificate_path do |f| %>
        <div class="modal-body">
          <div class="errors"></div>
          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, required: true, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :short_name %>
            <%= f.text_field :short_name, required: true, class: 'form-control' %>
          </div>
        </div>
        <%= f.submit class: "btn btn-primary pd-10" %>
        <%end%>
      </div>
      <div class="col-md-7">
        <p align="center">Employement Certificate List</p>
        <table class="table">
          <thead>
            <tr>
              <th>Employement Certificate Name</th>
              <th>Employement Certificate Short Name</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% @employment_certificates.each do |employment_certificate| %>
            <tr>
              <td><%= employment_certificate.name %></td>
              <td><%= employment_certificate.short_name %></td>
              <td class="action-link">
                <a href="" data-toggle="modal" data-target="#employment_certificate_id_<%= employment_certificate.id%>">
                  <i class="fa fa-user-circle-o"></i>
                </a>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>

<% @employment_certificates.each do |employment_certificate| %>
  <!-- Modal -->
  <div id="employment_certificate_id_<%= employment_certificate.id%>" class="modal fade" role="dialog">
    <div class="modal-dialog">

      <!-- Modal content-->
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
          <h4 class="modal-title">Employement Certificate</h4>
        </div>
        <div class="modal-body">
          <p>Employement Certificate Name : <span><%= employment_certificate.name %></span></p>
          <p>Employement Certificate Short Name : <span><%= employment_certificate.short_name %></span></p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        </div>
      </div>

    </div>
  </div>
<% end %>

